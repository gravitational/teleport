kind: role
metadata:
  name: rbac-session-read
spec:
  allow:
    logins:
    - root
    node_labels:
      '*': '*'
    rules:
    - resources:
      - session
      verbs:
      - list
      - read
  options:
    max_session_ttl: 8h0m0s
version: v3
