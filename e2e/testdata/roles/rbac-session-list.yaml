kind: role
metadata:
  name: rbac-session-list
spec:
  allow:
    logins:
    - root
    node_labels:
      '*': '*'
    rules:
    - resources:
      - session
      verbs:
      - list
  options:
    max_session_ttl: 8h0m0s
version: v3