{
  "name": "@gravitational/e2e",
  "version": "1.0.0",
  "type": "module",
  "description": "Playwright tests for e2e testing.",
  "scripts": {
    "test": "./run.sh",
    "type-check": "tsc --noEmit && tsc -p ../e/e2e/tsconfig.json --noEmit",
    "test:canonical-key": "tsx scripts/verify-canonical-key.ts",
    "show-report": "playwright show-report --port 0"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "cbor-x": "^1.6.6",
    "tsx": "^4.23.13",
    "undici": "^8.10.2"
  }
}
