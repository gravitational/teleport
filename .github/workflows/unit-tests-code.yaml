name: Unit Tests (Go)
run-name: Unit Tests (Go) - ${{ github.run_id }} - @${{ github.actor }}

on:
  push:
    branches:
      - master
      - branch/*
      - okraport/experiment-with-test-metrics
  pull_request:
    paths:
      - '.github/workflows/unit-tests-code.yaml'
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'build.assets/Makefile'
      - 'build.assets/Dockerfile*'
      - 'Makefile'
  merge_group:
    paths:
      - '.github/workflows/unit-tests-code.yaml'
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'build.assets/Makefile'
      - 'build.assets/Dockerfile*'
      - 'Makefile'

jobs:
  test:
    name: Unit Tests (Go)
    if: ${{ !startsWith(github.head_ref, 'dependabot/') }}
    runs-on: ubuntu-22.04-32core

    permissions:
      contents: read
      packages: read
      id-token: write

    container:
      image: ghcr.io/gravitational/teleport-buildbox:teleport19
      env:
        TELEPORT_ETCD_TEST: yes
        TELEPORT_ETCD_TEST_ENDPOINT: https://etcd0:2379
        TELEPORT_XAUTH_TEST: yes
        WEBASSETS_SKIP_BUILD: 1
      options: --cap-add=SYS_ADMIN --privileged

    services:
      etcd0:
        image: ghcr.io/gravitational/ci-etcd:3.3.9
        options: >-
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
          --add-host etcd0:127.0.0.1
        ports:
          - 2379:2379
          - 2380:2380
          - 3379:3379

    steps:
      - name: Checkout Teleport
        uses: actions/checkout@v6

      - name: Prepare workspace
        id: prepare
        uses: ./.github/actions/prepare-workspace

      - name: Mount debugfs
        run: mount -t debugfs none /sys/kernel/debug/

      # - name: Checkout shared-workflows
      #   uses: actions/checkout@v6
      #   with:
      #     repository: gravitational/shared-workflows
      #     path: .github/shared-workflows
      #     ref: 697d17be923e0182e6632be1fbc11c66a2dc72d5 # okraport experiment


      - name: Run tests
        timeout-minutes: 20
        run: make -j"$(nproc)" test-go-unit

      # - name: Normalize Test Results # TODO: move to a common file somewhere
      #   id: normalize_test_results
      #   if: always()
      #   continue-on-error: true
      #   run: |
      #     cd .github/shared-workflows/tools/ci-normalize && go run ./cmd junit \
      #       --meta $GITHUB_WORKSPACE/test-logs/ci-job-meta.json \
      #       --testcase $GITHUB_WORKSPACE/test-logs/testcase.jsonl \
      #       --suite $GITHUB_WORKSPACE/test-logs/suite.jsonl \
            # $GITHUB_WORKSPACE/test-logs/*.xml

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@a03048d87541d1d9fcf2ecf528a4a65ba9bd7838 # v5.0.0
        if: always()
        continue-on-error: true
        with:
          aws-region: us-west-2
          role-to-assume: arn:aws:iam::278576220453:role/tf-ci-test-artifacts-upload-gha-role
          role-session-name: gha-ci-test-artifacts-${{ github.event.repository.name }}-${{ github.run_number }}
          role-duration-seconds: 900 # Minimum by policy, we don't need it to be that high.

      - name: Normalize test results and push to s3
        uses: gravitational/shared-workflows/tools/ci-normalize@7a6b9f3a7d52c388d8c24f8edf279aa5ccc2f8d3
        if: always()
        continue-on-error: true
        with:
          s3-bucket: "s3://tp-278576220453-ci-test-artifacts-storage/ci-metrics"
          junit-files: "$GITHUB_WORKSPACE/test-logs/*.xml"


      #   # Install aws cli for testing
      # - name: Install AWS CLI v2
      #   if: always()
      #   continue-on-error: true
      #   run: |
      #     curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o awscliv2.zip
      #     unzip awscliv2.zip
      #     ./aws/install
      #     aws --version
      #     echo "outcome=${{ steps.normalize_test_results.outcome }}"
      #     echo "conclusion=${{ steps.normalize_test_results.conclusion }}"

      # - name: Upload test artifacts
      #   if: always() && steps.normalize_test_results.outcome == 'success'
      #   continue-on-error: true
      #   # In the future perhaps the CLI could pipe the results directly to s3.
      #   run: |
      #     META_FILE="$GITHUB_WORKSPACE/test-logs/ci-job-meta.json"

      #     urlencode() {
      #         string="$1"
      #         i=1
      #         while [ "$i" -le "$(printf "%s" "$string" | wc -c)" ]; do
      #             c=$(printf "%s" "$string" | cut -c "$i")
      #             case "$c" in
      #                 [a-zA-Z0-9.~_-])
      #                     printf "%s" "$c"
      #                     ;;
      #                 *)
      #                     printf '%%%02X' "'$c"
      #                     ;;
      #             esac
      #             i=$((i + 1))
      #         done
      #         printf "\n"
      #     }

      #     repo_raw=$(jq -r '.repository' $META_FILE)
      #     REPO=$(urlencode "$repo_raw")
      #     ID=$(jq -r '.id' $META_FILE)
      #     META_VERSION=$(jq -r '.canonical_meta_schema_version' $META_FILE)

      #     TIMESTAMP=$(jq -r '.timestamp' $META_FILE)
      #     YEAR=$(date -u -d "$TIMESTAMP" +"%Y")
      #     MONTH=$(date -u -d "$TIMESTAMP" +"%m")
      #     DAY=$(date -u -d "$TIMESTAMP" +"%d")

      #     BUCKET=tp-278576220453-ci-test-artifacts-storage
      #     aws s3 cp ${GITHUB_WORKSPACE}/test-logs/ci-job-meta.json "s3://tp-278576220453-ci-test-artifacts-storage/ci-metrics/jsonl/${META_VERSION}/jobs/repository=${REPO}/year=${YEAR}/month=${MONTH}/day=${DAY}/${TIMESTAMP}-job-${ID}.json"
      #     aws s3 cp ${GITHUB_WORKSPACE}/test-logs/testcase.jsonl "s3://${BUCKET}/ci-metrics/jsonl/${META_VERSION}/tests/repository=${REPO}/year=${YEAR}/month=${MONTH}/day=${DAY}/${TIMESTAMP}-tests-${ID}.jsonl"
      #     aws s3 cp ${GITHUB_WORKSPACE}/test-logs/suite.jsonl "s3://${BUCKET}/ci-metrics/jsonl/${META_VERSION}/suites/repository=${REPO}/year=${YEAR}/month=${MONTH}/day=${DAY}/${TIMESTAMP}-suites-${ID}.jsonl"
