name: Smoke test package rpm/deb
run-name: Smoke test package rpm/deb

on:
  merge_group:
    paths:
      - 'lib/autoupdate/**.go'
      - 'tool/teleport-update/**.go'
      - 'go.mod'
      - 'go.sum'
      - 'build.assets/Makefile'
      - 'build.assets/Dockerfile*'
      - 'Makefile'

jobs:
  build:
    name: Smoke test package rpm/deb
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running smoke tests for RPM/DEB"