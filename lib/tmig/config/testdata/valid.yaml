target:
  proxy: scoped.example.com:443
  identity: ./team-a-identity
migrations:
  - source:
      proxy: legacy-1.example.com:443
      identity: ./legacy-1-identity
    ssh:
      login: ec2-user
    mappings:
      - selector: { resource_group: team-a }
        scope: /dgxc/team-a
        install_suffix: dgxc-team-a
concurrency: 32
marker_prefix: tmig.teleport.dev
