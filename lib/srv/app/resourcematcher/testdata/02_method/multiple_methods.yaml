description: multiple methods
rules:
    - paths:
        - /api/**
      methods:
        - GET
        - HEAD
rules_desugared:
    - pred: |-
        path.match(
          literal("api",
            greedy())) &&
        contains(
          set("GET", "HEAD"), request.method)
identity:
    name: bob
    roles:
        - developer
cases:
    - request:
        method: HEAD
        path: /api/users
      expect:
        allowed: true
    - request:
        method: GET
        path: /api/users
      expect:
        allowed: true
    - request:
        method: POST
        path: /api/users
      expect:
        allowed: false
        deny_kind: teleport_request_not_allowed
    - request:
        method: DELETE
        path: /api
      expect:
        allowed: false
        deny_kind: teleport_request_not_allowed
