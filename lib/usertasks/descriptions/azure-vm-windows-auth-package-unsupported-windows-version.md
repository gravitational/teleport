# Unsupported Windows version

The Teleport Windows Authentication Package requires Windows Server 2016, Windows 10, or later.
This VM is running an older, unsupported version of Windows.

To resolve this, upgrade the VM to a supported Windows version. If this VM should not be
enrolled, exclude it from the discovery config's Azure matcher, for example using a tag filter.
