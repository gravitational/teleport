apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: teleportworkloadidentities.resources.teleport.dev
spec:
  group: resources.teleport.dev
  names:
    kind: TeleportWorkloadIdentity
    listKind: TeleportWorkloadIdentityList
    plural: teleportworkloadidentities
    shortNames:
    - workloadidentity
    - workloadidentities
    singular: teleportworkloadidentity
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: WorkloadIdentity is the Schema for the workloadidentities API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WorkloadIdentity resource definition  from Teleport
            properties:
              rules:
                description: Rules holds the authorization rules that must pass for
                  this WorkloadIdentity to be used. See [WorkloadIdentityRules] for
                  further documentation.
                nullable: true
                properties:
                  allow:
                    description: Allow is a list of rules, each containing a set of
                      attribute matchers. For a rule to match, all matchers within
                      the rule must match. If rules are specified, then at least one
                      rule must match for issuance to be permitted. If no rules are
                      specified, issuance is permitted.
                    items:
                      properties:
                        conditions:
                          items:
                            properties:
                              attribute:
                                type: string
                              equals:
                                type: string
                            type: object
                          nullable: true
                          type: array
                      type: object
                    nullable: true
                    type: array
                  deny:
                    description: Deny is a set of rules, each containing a set of
                      attribute matchers. For a rule to match, all matchers within
                      the rule must match. If rules are specified, then all rules
                      must not match for issuance to be permitted. If no rules are
                      specified, issuance is permitted.
                    items:
                      properties:
                        conditions:
                          items:
                            properties:
                              attribute:
                                type: string
                              equals:
                                type: string
                            type: object
                          nullable: true
                          type: array
                      type: object
                    nullable: true
                    type: array
                type: object
              spiffe:
                description: SPIFFE holds configuration for the structure of SPIFFE-compatible
                  workload identity credentials for this WorkloadIdentity. See [WorkloadIdentitySPIFFE]
                  for further documentation.
                nullable: true
                properties:
                  id:
                    description: 'Id is the path of the SPIFFE ID that will be issued
                      in workload identity credentials when this WorkloadIdentity
                      is used. It can be templated using attributes.  Examples: -
                      `/no/templating/used` -> `spiffe://example.teleport.sh/no/templating/used`
                      - `/gitlab/{{ join.gitlab.project_path }}` -> `spiffe://example.teleport.sh/gitlab/org/project`'
                    type: string
                type: object
            type: object
          status:
            description: Status defines the observed state of the Teleport resource
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              teleportResourceID:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
