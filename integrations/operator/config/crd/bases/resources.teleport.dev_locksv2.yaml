apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: teleportlocksv2.resources.teleport.dev
spec:
  group: resources.teleport.dev
  names:
    kind: TeleportLockV2
    listKind: TeleportLockV2List
    plural: teleportlocksv2
    shortNames:
    - lockv2
    - locksv2
    singular: teleportlockv2
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: LockV2 is the Schema for the locksv2 API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Lock resource definition v2 from Teleport
            properties:
              created_at:
                description: CreatedAt is the date time that the lock was created.
                format: date-time
                type: string
              created_by:
                description: CreatedBy is the username of the author of the lock.
                type: string
              expires:
                description: Expires if set specifies when the lock ceases to be in
                  force.
                format: date-time
                type: string
              message:
                description: Message is the message displayed to locked-out users.
                type: string
              target:
                description: Target describes the set of interactions that the lock
                  applies to.
                properties:
                  access_request:
                    description: AccessRequest specifies the UUID of an Access Request.
                    type: string
                  bot_instance_id:
                    description: BotInstanceID is the bot instance ID if this is a
                      bot identity and is ignored otherwise.
                    type: string
                  device:
                    description: Device is the device ID of a trusted device. Requires
                      Teleport Enterprise.
                    type: string
                  join_token:
                    description: JoinToken is the name of the join token used when
                      this identity originally joined. This is only valid for bot
                      identities, and cannot be used to target `token`-joined bots.
                    type: string
                  linux_desktop:
                    description: LinuxDesktop specifies the name of a Linux desktop.
                    type: string
                  login:
                    description: Login specifies the name of a local UNIX user.
                    type: string
                  mfa_device:
                    description: MFADevice specifies the UUID of a user MFA device.
                    type: string
                  role:
                    description: Role specifies the name of an RBAC role known to
                      the root cluster. In remote clusters, this constraint is evaluated
                      before translating to local roles.
                    type: string
                  server_id:
                    description: ServerID is the host id of the Teleport instance.
                    type: string
                  user:
                    description: User specifies the name of a Teleport user.
                    type: string
                  windows_desktop:
                    description: WindowsDesktop specifies the name of a Windows desktop.
                    type: string
                type: object
            type: object
          status:
            description: Status defines the observed state of the Teleport resource
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              teleportResourceID:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
