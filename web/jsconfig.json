{
    "compilerOptions": {
        "baseUrl": "./src/app",  // all paths are relative to the baseUrl
        "paths": {
            "app/*" : ["*"]   // resolve any `~/foo/bar` to `<baseUrl>/foo/bar`
        }
    }
}