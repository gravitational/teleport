kind: role
spec:
  allow:
    # Define OS users.
    logins:
    - osUser1
    - osUser2
    rules:
    # Rule that allows users to define their own logins.
    - resources:
      - user
      verbs:
      - update
