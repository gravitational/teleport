[Unit]
Description=Gravitational Teleport Service
After=network.target

[Service]
Type=simple
Restart=always
ExecStart=/usr/local/bin/teleport --config=/etc/teleport.yaml

[Install]
WantedBy=local.target
