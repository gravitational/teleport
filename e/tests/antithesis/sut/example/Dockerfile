# Config image for Antithesis.
# Bundles docker-compose.yaml, .env, and teleport config files so Antithesis
# can discover the topology and mount configs into SUT containers at run time.
FROM scratch
COPY . /
