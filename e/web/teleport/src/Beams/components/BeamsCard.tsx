import styled from 'styled-components';

import { Box } from 'design';

export const BeamsCard = styled(Box).attrs({
  padding: 6,
  borderRadius: 3,
  border: '1px solid',
  borderColor: 'interactive.tonal.neutral.2',
  backgroundColor: 'levels.surface',
})``;
