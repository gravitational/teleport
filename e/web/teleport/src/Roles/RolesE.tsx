import { RolesContainer as Roles } from 'teleport/Roles';

export const RolesE = () => {
  // TODO (avatus) this is where we will import the access graph
  // component and an updater callback
  return <Roles />;
};
