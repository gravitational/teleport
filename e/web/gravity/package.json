{
  "name": "@gravitational/gravity.e",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "gravity-build --progress --dev; gravity-start",
    "build": "rm -rf ./dist; mkdir ./dist; gravity-build --progress --bail",
    "test": "jest",
    "tdd": "jest . --watch"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "@gravitational/gravity": "^1.0.0",
    "cross-env": "5.0.5"
  },
  "devDependencies": {
    "@gravitational/build": "^1.0.0"
  }
}
