# This is a dummy workflow on master. It currently only is used on branch/v14
# but it needs to exist on master for GitHub Actions to allow a workflow
# dispatch to it.
# TODO(camscale): REMOVE IN 18.0.0, assuming GitHub has increased the workflow
# call limit to 50 (from 20) before v15/v16 exceed 20 called workflows.

name: Tag Build Teleport
on:
  workflow_dispatch:
jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - name: Greeting
        run: echo "This is a dummy build to placate GitHub."
