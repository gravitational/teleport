teleport:
  nodename: graviton-node
  auth_token: hello
  auth_servers:
  - 127.0.0.1:5000
  data_dir: .data
proxy_service:
  enabled: "no"
auth_service:
  enabled: "no"
ssh_service:
  enabled: "yes"
