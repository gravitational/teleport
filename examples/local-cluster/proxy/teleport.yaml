version: v3
teleport:
  nodename: graviton-proxy
  join_params:
    method: token
    token_name: hello
  auth_server: 127.0.0.1:5000
  data_dir: .data
proxy_service:
  enabled: "yes"
auth_service:
  enabled: "no"
ssh_service:
  enabled: "no"
