kind: role
metadata:
  name: access-plugin-auto-approve
spec:
  allow:
    rules:
      - resources: ['access_request']
        verbs: ['list','read','update']
version: v4
---
kind: user
metadata:
  name: access-plugin-auto-approve
spec:
  roles: ['access-plugin-auto-approve']
version: v2
