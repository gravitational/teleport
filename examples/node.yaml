log:
  output: console
  severity: INFO

data_dir: /var/lib/teleport/node # directory where teleport stores it's state (SSH keys and database data)

hostname: localhost # SSH hostname of this node

ssh:
  token: /tmp/token
  # enable simple ssh endpoint
  enabled: true
  addr: tcp://localhost:33021

auth_servers: 
  - tcp://localhost:33000

