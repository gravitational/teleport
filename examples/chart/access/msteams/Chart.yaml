apiVersion: v2
name: teleport-plugin-msteams
description: A Helm chart for the Teleport MsTeams Plugin
type: application
version: &version "19.0.0-carheden.helm-proxy-only.5"
appVersion: *version

dependencies:
  - name: tbot
    version: *version
    condition: tbot.enabled
