.version: &version "18.6.4-dev.dustinspecker.workloadclusters.2"

apiVersion: v2
name: teleport-plugin-msteams
description: A Helm chart for the Teleport MsTeams Plugin
type: application
version: *version
appVersion: *version

dependencies:
  - name: tbot
    version: *version
    condition: tbot.enabled
