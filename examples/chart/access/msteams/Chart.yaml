apiVersion: v2
name: teleport-plugin-msteams
description: A Helm chart for the Teleport MsTeams Plugin
type: application
version: &version "18.11.0-dev.alexh.3"
appVersion: *version

dependencies:
  - name: tbot
    version: *version
    condition: tbot.enabled
