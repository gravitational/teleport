apiVersion: v2
name: teleport-plugin-slack
description: A Helm chart for the Teleport Slack Plugin
type: application
version: &version "19.0.0-aadc.730.4"
appVersion: *version

dependencies:
  - name: tbot
    version: *version
    condition: tbot.enabled
