.version: &version "18.6.1-dev.pnpm-10-27-0.1"

apiVersion: v2
name: teleport-plugin-jira
description: A Helm chart for the Teleport Jira Plugin
type: application
version: *version
appVersion: *version

dependencies:
  - name: tbot
    version: *version
    condition: tbot.enabled
