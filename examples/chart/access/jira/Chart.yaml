apiVersion: v2
name: teleport-plugin-jira
description: A Helm chart for the Teleport Jira Plugin
type: application
version: &version "19.0.0-dev.stephen.5"
appVersion: *version

dependencies:
  - name: tbot
    version: *version
    condition: tbot.enabled
