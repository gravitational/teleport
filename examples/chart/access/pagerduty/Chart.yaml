apiVersion: v2
name: teleport-plugin-pagerduty
description: A Helm chart for the Teleport Pagerduty Plugin
type: application
version: &version "19.0.0-recordingexport.dev.4"
appVersion: *version

dependencies:
  - name: tbot
    version: *version
    condition: tbot.enabled
