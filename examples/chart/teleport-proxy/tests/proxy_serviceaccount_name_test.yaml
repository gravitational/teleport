suite: Proxy ServiceAccount Name
templates:
  - proxy.yaml
tests:
  - it: uses serviceAccount.name verbatim when set
    values:
      - ../.lint/service-account.yaml
    documentIndex: 6
    asserts:
      - isKind:
          of: ServiceAccount
      - equal:
          path: metadata.name
          value: helm-lint
