---
title: Database Access AWS IAM Reference
description: AWS IAM policies for Teleport database access.
---

Teleport Database Services require IAM permissions for various tasks,
including discovering endpoints and instance metadata, generating IAM
authentication tokens, and assuming IAM roles.

You can generate the permissions with the [`teleport db configure
aws print-iam`](../../database-access/reference/cli.mdx#teleport-db-configure--awsprint-iam)
command. For example, the following command would generate and print the IAM
policies:

```code
$ teleport db configure aws print-iam --types rds,redshift --role db-service-role
```

For further details on each database type, please refer to the sections below.

## RDS

In this section, you will find information about the necessary IAM permissions
that are required for RDS instances, Aurora clusters, and RDS Proxies.

Teleport Database Service can automatically manage IAM policies for RDS access.
To use this feature, Teleport requires `iam:PutRolePolicy` or
`iam:PutUserPolicy` permissions to grant itself permissions to generate IAM
Authentication tokens for registered databases.

If you prefer to manage IAM identities on your own, please refer to [this sub
section](#manage-iam-identities-yourself-for-rds) which outlines the required
IAM permissions.

### Teleport-managed IAM identities for RDS

The required IAM permissions are slightly different depending on the IAM
identity Teleport is managing:

<Tabs>
  <TabItem label="IAM role">
  Use this policy if your Teleport Database Service runs as an IAM role (for
  example, on an EC2 instance with an attached IAM role):
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "rds:DescribeDBInstances",
                  "rds:DescribeDBClusters",
                  "rds:ModifyDBInstance",
                  "rds:ModifyDBCluster",
                  "rds:DescribeDBProxies",
                  "rds:DescribeDBProxyEndpoints",
                  "rds:ListTagsForResource",
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetRolePolicy",
                  "iam:PutRolePolicy",
                  "iam:DeleteRolePolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:role/<Var name="sample-role"/>"
          }
      ]
  }
  ```

  (!docs/pages/includes/database-access/reference/rds-action-details.mdx!)

  To improve security, use the following boundary policy that limits Teleport's
  self-granting capability on the IAM role:
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "rds:DescribeDBInstances",
                  "rds:DescribeDBClusters",
                  "rds:ModifyDBInstance",
                  "rds:ModifyDBCluster",
                  "rds:DescribeDBProxies",
                  "rds:DescribeDBProxyEndpoints",
                  "rds:ListTagsForResource",
                  "rds-db:connect"
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetRolePolicy",
                  "iam:PutRolePolicy",
                  "iam:DeleteRolePolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:role/<Var name="sample-role"/>"
          }
      ]
  }
  ```
  </TabItem>

  <TabItem label="IAM user">
  Use this policy if you're connecting to RDS instances and Aurora clusters,
  and your Teleport Database Service instance runs as an IAM user (for example,
  uses an AWS credentials file):
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "rds:DescribeDBInstances",
                  "rds:DescribeDBClusters",
                  "rds:ModifyDBInstance",
                  "rds:ModifyDBCluster",
                  "rds:DescribeDBProxies",
                  "rds:DescribeDBProxyEndpoints",
                  "rds:ListTagsForResource",
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetUserPolicy",
                  "iam:PutUserPolicy",
                  "iam:DeleteUserPolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:user/<Var name="sample-user"/>"
          }
      ]
  }
  ```

  (!docs/pages/includes/database-access/reference/rds-action-details.mdx!)

  To improve security, use the following boundary policy that limits Teleport's
  self-granting capability on the IAM user:
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "rds:DescribeDBInstances",
                  "rds:DescribeDBClusters",
                  "rds:ModifyDBInstance",
                  "rds:ModifyDBCluster",
                  "rds:DescribeDBProxies",
                  "rds:DescribeDBProxyEndpoints",
                  "rds:ListTagsForResource",
                  "rds-db:connect"
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetUserPolicy",
                  "iam:PutUserPolicy",
                  "iam:DeleteUserPolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:user/<Var name="sample-user"/>"
          }
      ]
  }
  ```
  </TabItem>
</Tabs>


### Manage IAM identities yourself for RDS

To connect to an RDS database, the Database Service instance's IAM identity
needs to have `rds-db:connect` permissions for it. Here is a sample policy:
```json
{
   "Version": "2012-10-17",
   "Statement": [
      {
         "Effect": "Allow",
         "Action": [
             "rds-db:connect"
         ],
         "Resource": [
             "arn:aws:rds-db:<Var name="us-east-2"/>:<Var name="aws-account-id"/>:dbuser:db-ABCDEFGHIJKL01234/*",
             "arn:aws:rds-db:<Var name="us-east-2"/>:<Var name="aws-account-id"/>:dbuser:cluster-ABCDEFGHIJKL01234/*",
             "arn:aws:rds-db:<Var name="us-east-2"/>:<Var name="aws-account-id"/>:dbuser:prx-ABCDEFGHIJKL01234/*"
         ]
      }
   ]
}
```

The resource ARN in the policy has the following format:

```
arn:aws:rds-db:<region>:<account-id>:dbuser:<resource-id>/<db-user>
```

| Parameter     | Description |
| ------------- | ----------- |
| `region`      | AWS region where the database cluster is deployed. |
| `account-id`  | AWS account ID the database cluster is deployed under. |
| `resource-id` | Database AWS resource identifier: <br/>- `db-XXX` for RDS instances.<br/>- `cluster-XXX` for Aurora clusters.<br/>- `prx-XXX` for RDS Proxy.<br/>Can be found under Configuration section in the RDS control panel. |
| `db-user`     | Database user to associate with IAM authentication. Can be a wildcard. |

See [Creating and using an IAM policy for IAM database access](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/UsingWithRDS.IAMDBAuth.IAMPolicy.html)
for more information.

If auto-discovery is used to register the databases, please also ensure the IAM
identity has the following permissions:
```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "rds:DescribeDBInstances",
                "rds:DescribeDBClusters",
                "rds:ModifyDBInstance",
                "rds:ModifyDBCluster",
                "rds:DescribeDBProxies",
                "rds:DescribeDBProxyEndpoints",
                "rds:ListTagsForResource",
            ],
            "Resource": "*"
        }
    ]
}
```

(!docs/pages/includes/database-access/reference/rds-action-details.mdx!)

(!docs/pages/includes/database-access/reference/boundary-policy-note.mdx!)

## Redshift

As mentioned in the [Redshift
guide](../guides/postgres-redshift.mdx#step-36-create-an-iam-role-for-user-access-optional),
Teleport supports IAM authentication as either an existing database user or an IAM
role for Redshift.

When authenticating as an existing database user, Teleport Database Service can
automatically manage IAM policies for Redshift access. To use this feature, Teleport
requires `iam:PutRolePolicy` or `iam:PutUserPolicy` permissions to grant itself
permissions to generate IAM Authentication tokens for registered databases. If
you prefer to manage IAM identities on your own, please refer to [this sub
section](#manage-iam-identities-yourself-for-redshift) which outlines the
required IAM permissions.

When authenticating as an IAM role, please use the IAM permissions outlined in
[this sub section](#iam-authentication-as-iam-role-for-redshift).

### Teleport-managed IAM identities for Redshift

The required IAM permissions are slightly different depending on the IAM
identity Teleport is managing:

<Tabs>
  <TabItem label="IAM role">
  Use this permission boundary if your Teleport Database Service instance runs
  as an IAM role (for example, on an EC2 instance with an attached IAM role):
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "redshift:DescribeClusters",
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetRolePolicy",
                  "iam:PutRolePolicy",
                  "iam:DeleteRolePolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:role/<Var name="sample-role"/>"
          }
      ]
  }
  ```

  To improve security, use the following boundary policy that limits Teleport's
  self-granting capability on the IAM role:
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "redshift:DescribeClusters",
                  "redshift:GetClusterCredentials",
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetRolePolicy",
                  "iam:PutRolePolicy",
                  "iam:DeleteRolePolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:role/<Var name="sample-role"/>"
          }
      ]
  }
  ```

  </TabItem>
  <TabItem label="IAM user">
  Use this permission boundary if your Teleport Database Service runs as an IAM
  user (for example, it uses an AWS credentials file):
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "redshift:DescribeClusters",
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetUserPolicy",
                  "iam:PutUserPolicy",
                  "iam:DeleteUserPolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:user/<Var name="sample-user"/>"
          }
      ]
  }
  ```

  To improve security, use the following boundary policy that limits Teleport's
  self-granting capability on the IAM user:
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "redshift:DescribeClusters",
                  "redshift:GetClusterCredentials"
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetUserPolicy",
                  "iam:PutUserPolicy",
                  "iam:DeleteUserPolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:user/<Var name="sample-user"/>"
          }
      ]
  }
  ```
  </TabItem>
</Tabs>

### Manage IAM identities yourself for Redshift

In order to authorize Teleport to generate temporary IAM tokens, create an IAM
role with the `GetClusterCredentials` permission. Here is a sample policy:
```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": "redshift:GetClusterCredentials",
            "Resource": [
                "arn:aws:redshift:<Var name="us-east-2"/>:<Var name="aws-account-id"/>:dbuser:<Var name="cluster_id"/>/*",
                "arn:aws:redshift:<Var name="us-east-2"/>:<Var name="aws-account-id"/>:dbname:<Var name="cluster_id"/>/*",
                "arn:aws:redshift:<Var name="us-east-2"/>:<Var name="aws-account-id"/>:dbgroup:<Var name="cluster_id"/>/*"
            ]
        }
    ]
}
```

The resource ARN string has the following format:

```
arn:aws:redshift:<region>:<account-id>:<resource>:<cluster-id>/<name>
```

Parameters:
| Parameter     | Description |
| ------------- | ----------- |
| `region`      | AWS region where your Redshift cluster is deployed, or a wildcard. |
| `account-id`  | ID of the AWS account where the Redshift cluster is deployed. |
| `resource`    | One of `dbuser`, `dbname` or `dbgroup` to restrict access to database accounts, names or groups respectively. |
| `cluster-id`  | Redshift cluster identifier, or a wildcard. |
| `name`        | Name of a particular database account, name or group (depending on the `resource`), or a wildcard. |

See [Create an IAM role or user with permissions to call
GetClusterCredentials](https://docs.aws.amazon.com/redshift/latest/mgmt/generating-iam-credentials-role-permissions.html)
for more information.

If auto-discovery is used to register the databases, please also ensure the IAM
identity has the following permissions:
```
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "redshift:DescribeClusters",
            ],
            "Resource": "*"
        }
    ]
}
```

(!docs/pages/includes/database-access/reference/boundary-policy-note.mdx!)

### IAM authentication as IAM role for Redshift

When authenticating Redshift as an IAM role, Teleport Database Service must be
able to assume this IAM role.

(!docs/pages/includes/database-access/iam_role_trust_relationship.mdx role1="db-service-role" role2="redshift-access-role"!)

The Redshift access IAM role requires the following permissions:
```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": "redshift:GetClusterCredentialsWithIAM",
            "Resource": "arn:aws:redshift:<Var name="us-east-2"/>:<Var name="aws-account-id"/>:dbname:<Var name="cluster-id"/>/*"
        }
    ]
}
```

## ElastiCache/MemoryDB

ElastiCache and MemoryDB supports IAM authentication for Redis engine version
7.0 or above. Teleport Database Service can automatically manage IAM policies
for ElastiCache and MemoryDB that use IAM authentication. To use this feature,
Teleport requires `iam:PutRolePolicy` or `iam:PutUserPolicy` permissions to
grant itself permissions to generate IAM Authentication tokens for registered
databases. If you prefer to manage IAM identities on your own, please refer to
[this sub section](#manage-iam-identities-yourself-for-elasticachememorydb)
which outlines the required IAM permissions.

Teleport also supports automatic password rotations for ElastiCache and
MemoryDB users using secrets manager, when IAM authentication is not available.
Please use the IAM permissions outlined in [this sub
section](#auto-password-rotation-for-elasticachememorydb).

### Teleport-managed IAM identities for ElastiCache/MemoryDB

The required IAM permissions are slightly different depending on the IAM
identity Teleport is managing:

<Tabs>
  <TabItem label="IAM role">
  Use this permission boundary if your Teleport Database Service instance runs
  as an IAM role (for example, on an EC2 instance with an attached IAM role):
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "elasticache:ListTagsForResource",
                  "elasticache:DescribeReplicationGroups",
                  "elasticache:DescribeCacheClusters",
                  "elasticache:DescribeCacheSubnetGroups",
                  "elasticache:DescribeUsers"
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "memorydb:ListTags",
                  "memorydb:DescribeClusters",
                  "memorydb:DescribeSubnetGroups",
                  "memorydb:DescribeUsers",
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetRolePolicy",
                  "iam:PutRolePolicy",
                  "iam:DeleteRolePolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:role/<Var name="sample-role"/>"
          }
      ]
  }
  ```

  To improve security, use the following boundary policy that limits Teleport's
  self-granting capability on the IAM role:
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "elasticache:ListTagsForResource",
                  "elasticache:DescribeReplicationGroups",
                  "elasticache:DescribeCacheClusters",
                  "elasticache:DescribeCacheSubnetGroups",
                  "elasticache:DescribeUsers",
                  "elasticache:Connect",
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "memorydb:ListTags",
                  "memorydb:DescribeClusters",
                  "memorydb:DescribeSubnetGroups",
                  "memorydb:DescribeUsers",
                  "memorydb:Connect"
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetRolePolicy",
                  "iam:PutRolePolicy",
                  "iam:DeleteRolePolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:role/<Var name="sample-role"/>"
          }
      ]
  }
  ```

  </TabItem>
  <TabItem label="IAM user">
  Use this permission boundary if your Teleport Database Service runs as an IAM
  user (for example, it uses an AWS credentials file):
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "elasticache:ListTagsForResource",
                  "elasticache:DescribeReplicationGroups",
                  "elasticache:DescribeCacheClusters",
                  "elasticache:DescribeCacheSubnetGroups",
                  "elasticache:DescribeUsers"
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "memorydb:ListTags",
                  "memorydb:DescribeClusters",
                  "memorydb:DescribeSubnetGroups",
                  "memorydb:DescribeUsers"
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetUserPolicy",
                  "iam:PutUserPolicy",
                  "iam:DeleteUserPolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:user/<Var name="sample-user"/>"
          }
      ]
  }
  ```

  To improve security, use the following boundary policy that limits Teleport's
  self-granting capability on the IAM user:
  ```json
  {
      "Version": "2012-10-17",
      "Statement": [
          {
              "Effect": "Allow",
              "Action": [
                  "elasticache:ListTagsForResource",
                  "elasticache:DescribeReplicationGroups",
                  "elasticache:DescribeCacheClusters",
                  "elasticache:DescribeCacheSubnetGroups",
                  "elasticache:DescribeUsers",
                  "elasticache:Connect",
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "memorydb:ListTags",
                  "memorydb:DescribeClusters",
                  "memorydb:DescribeSubnetGroups",
                  "memorydb:DescribeUsers",
                  "memorydb:Connect"
              ],
              "Resource": "*"
          },
          {
              "Effect": "Allow",
              "Action": [
                  "iam:GetUserPolicy",
                  "iam:PutUserPolicy",
                  "iam:DeleteUserPolicy"
              ],
              "Resource": "arn:aws:iam::<Var name="aws-account-id"/>:user/<Var name="sample-user"/>"
          }
      ]
  }
  ```
  </TabItem>
</Tabs>

### Manage IAM identities yourself for ElastiCache/MemoryDB

In order to authorize Teleport to generate temporary IAM tokens, create an IAM
role with the `Connect` permission. Here is a sample policy:
```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "elasticache:DescribeUsers",
                "memorydb:DescribeUsers"
            ],
            "Resource": "*"
        },
        {
            "Effect": "Allow",
            "Action": "elasticache:Connect",
            "Resource": [
                "arn:aws:elasticache:<Var name="us-east-2"/>:<Var name="aws-account-id"/>:replicationgroup:<Var name="replication-group"/>",
                "arn:aws:elasticache:<Var name="us-east-2"/>:<Var name="aws-account-id"/>:user:*"
            ]
        },
        {
            "Effect": "Allow",
            "Action": "memorydb:Connect",
            "Resource": [
                "arn:aws:memorydb:<Var name="us-east-2"/>:<Var name="aws-account-id"/>:cluster/<Var name="cluster-name"/>",
                "arn:aws:memorydb:<Var name="us-east-2"/>:<Var name="aws-account-id"/>:user/*"
            ],
        }
    ]
}
```

See [Authenticating with IAM for
ElastiCache](https://docs.aws.amazon.com/AmazonElastiCache/latest/red-ug/auth-iam.html)
and [Authenticating with IAM for
MemoryDB](https://docs.aws.amazon.com/memorydb/latest/devguide/auth-iam.html)
for more information.

If auto-discovery is used to register the databases, please also ensure the IAM
identity has the following permissions:
```
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "elasticache:ListTagsForResource",
                "elasticache:DescribeReplicationGroups",
                "elasticache:DescribeCacheClusters",
                "elasticache:DescribeCacheSubnetGroups",
            ],
            "Resource": "*"
        },
        {
            "Effect": "Allow",
            "Action": [
                "memorydb:ListTags",
                "memorydb:DescribeClusters",
                "memorydb:DescribeSubnetGroups",
            ],
            "Resource": "*"
        },
    ]
}
```

(!docs/pages/includes/database-access/reference/boundary-policy-note.mdx!)

### Auto-password rotation for ElastiCache/MemoryDB

If any ElastiCache or MemoryDB users are tagged to be managed by Teleport,
below are the IAM permissions required for managing the ElastiCache or MemoryDB
users:

```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "elasticache:DescribeUsers",
                "elasticache:ModifyUser"
            ],
            "Resource": [
                "*"
            ]
        },
        {
            "Effect": "Allow",
            "Action": [
                "memorydb:DescribeUsers",
                "memorydb:UpdateUser"
            ],
            "Resource": [
                "*"
            ]
        },
        {
            "Effect": "Allow",
            "Action": [
                "secretsmanager:DescribeSecret",
                "secretsmanager:CreateSecret",
                "secretsmanager:UpdateSecret",
                "secretsmanager:DeleteSecret",
                "secretsmanager:GetSecretValue",
                "secretsmanager:PutSecretValue",
                "secretsmanager:TagResource"
            ],
            "Resource": [
                "arn:aws:secretsmanager:*:<Var name="aws-account-id"/>:secret:teleport/*"
            ]
        }
    ]
}
```

If auto-discovery is used to register the databases, please also ensure the IAM
identity has the following permissions:
```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "elasticache:ListTagsForResource",
                "elasticache:DescribeReplicationGroups",
                "elasticache:DescribeCacheClusters",
                "elasticache:DescribeCacheSubnetGroups",
            ],
            "Resource": [
                "*"
            ]
        },
        {
            "Effect": "Allow",
            "Action": [
                "memorydb:ListTags",
                "memorydb:DescribeClusters",
                "memorydb:DescribeSubnetGroups",
            ],
            "Resource": [
                "*"
            ]
        }
    ]
}
```

If any custom key prefix or KMS key ID is used in the static configuration, add
the following to the IAM policy:
```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "secretsmanager:DescribeSecret",
                "secretsmanager:CreateSecret",
                "secretsmanager:UpdateSecret",
                "secretsmanager:DeleteSecret",
                "secretsmanager:GetSecretValue",
                "secretsmanager:PutSecretValue",
                "secretsmanager:TagResource"
            ],
            "Resource": [
                "arn:aws:secretsmanager:*:<Var name="aws-account-id"/>:secret:<Var name="my-prefix"/>/*"
            ]
        },
        {
            "Effect": "Allow",
            "Action": [
                "kms:GenerateDataKey",
                "kms:Decrypt"
            ],
            "Resource": [
                "arn:aws:kms:*:<Var name="aws-account-id"/>:key/<Var name="my-kms-id"/>",
            ]
        }
    ]
}
```

(!docs/pages/includes/database-access/reference/boundary-policy-note.mdx!)
