The {{serviceName}} MCP server uses {{token}} to access {{serviceName}} and runs
on a local endpoint reachable by Teleport. Teleport proxies all client requests
to the server, which interacts with {{serviceName}} using the permissions
granted to the integration.

