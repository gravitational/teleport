Now configure your MCP clients to access the MCP server via Teleport, for example:
```code
$ tsh mcp config {{service}}-mcp --client-config claude
```

After configuring your MCP client, you will find {{serviceName}}-related tools from
`teleport-mcp-{{service}}-mcp`. You can now use these tools to interactive with
{{serviceName}} via Teleport in your MCP clients:
