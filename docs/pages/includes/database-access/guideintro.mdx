Access to {{ dbName }} can be provided by [Teleport Database
Access](../../database-access/introduction.mdx). This allows for
fine-grain access control through [Teleport's RBAC](../../database-access/rbac.mdx).

In this guide, you will:

1. Configure an {{ dbConfigure }}.
1. Join the {{ dbName }} database to your Teleport cluster.
1. Connect to the {{ dbName }} database via the Teleport Database Service.