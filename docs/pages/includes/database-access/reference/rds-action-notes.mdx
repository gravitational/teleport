`rds:*DBInstance*`, `rds:*DBCluster*`, and `rds:*DBProxy*` are permissions
required for RDS instances, Aurora clusters, and RDS Proxies, respectively. You
can omit the other permissions if only some are used.
