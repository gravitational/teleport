Databases discovered by the Teleport Discovery Service should be registered with
complete metadata, so you can also omit the `{{ sid }}` permissions if all of
your AWS databases are being auto-discovered.
