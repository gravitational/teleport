## tsh app ls

Lists available applications.

```bash
tsh app ls
```

## tsh app login

Retrieves short-lived X.509 certificate for CLI [application access](https://goteleport.com/docs/application-access/introduction/).

```bash
tsh app login grafana
```

## tsh app logout

Removes CLI application access certificate.

```bash
# Log out of a particular app.
tsh app logout grafana

# Log out of all apps.
tsh app logout
```

## tsh app config

Prints application connection information.

```bash
# Print app information in a table form.
tsh app config

# Print information for a particular app.
tsh app config grafana

# Print an example curl command.
tsh app config --format=curl

# Construct a curl command.
curl $(tsh app config --format=uri) \
  --cacert $(tsh app config --format=ca) \
  --cert $(tsh app config --format=cert) \
  --key $(tsh app config --format=key)
```

<table>
  <thead>
    <tr>
      <td>Flag</td>
      <td>Description</td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`--format`</td>
      <td>Optional print format, one of: `uri` to print app address, `ca` to print CA cert path, `cert` to print cert path, `key` print key path, `curl` to print example curl command.</td>
    </tr>
  </tbody>
</table>