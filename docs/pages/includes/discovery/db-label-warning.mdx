
<Admonition type="warning">
Use more specific label selectors when needed so that a Database Service
instance only matches databases it can actually reach. Broad selectors can
cause unreachable databases to be registered and lead to connection failures.
</Admonition>