---
title: "Step 3 - Set Up Roles"
description: "Configure role-based access controls in your Teleport cluster."
toc_max_heading_level: 2
tags:
  - get-started
---

import Button from '@site/src/components/Button';
import Icon from '@site/src/components/Icon';

Teleport uses **Role-Based Access Control (RBAC)** to determine who can access what across your infrastructure.

Every Teleport user is assigned one or more roles. These roles define the user's permissions, such as which infrastructure resources they can access, whether they can request temporary elevated access, and whether they can edit cluster settings or view session recordings.

## Preset roles

Teleport ships with several preset roles to help you get started quickly. You likely have at least the `access` and `editor` roles assigned to your admin user.

| **Role**   | **Description**                                                         |
|------------|-------------------------------------------------------------------------|
| `access`   | Grants access to infrastructure resources.                              |
| `editor`   | Allows editing cluster configuration (e.g., roles, connectors).         |
| `auditor`  | Grants read-only access to audit logs, events, and session recordings.  |

<details>
<summary>View the full list of preset roles including Enterprise</summary>

(!docs/pages/includes/preset-roles-table.mdx!)

</details>

You can view all available roles by navigating to **Zero Trust Access** > **Roles** in the Web UI.

## When to create custom roles

The preset roles cover broad access patterns, but production clusters typically need custom roles to enforce least-privilege access. Consider creating custom roles when you need to:

- **Limit access by label**: Allow a team to access only resources tagged `env: production` or `team: platform`.
- **Restrict OS logins**: Permit a user to SSH as `ubuntu` but not `root`.
- **Define approvers**: Allow a manager to approve Access Requests without having direct access to resources.
- **Separate read from write**: Give auditors read-only access to session recordings without cluster edit permissions.

For a hands-on introduction to writing roles and assigning them to users, follow the [Teleport Role Demo](../zero-trust-access/rbac-get-started/role-demo.mdx).

For a conceptual overview of how roles, labels, and templates work together, read [Get Started with Role-Based Access Control](../zero-trust-access/rbac-get-started/rbac-get-started.mdx).

## Next step: Set up SSO

In the next step, you will connect Teleport to your identity provider so users can sign in with their existing credentials, and roles are assigned automatically based on group membership.

<div style={{ marginTop: 'var(--m-4)' }}>
  <Button style={{ padding: '0 var(--m-2)' }} as="link" href="../sso/" variant="primary" shape="lg">Step 4 - Set up SSO <Icon name="arrowRight" inline size="sm"/></Button>
</div>
