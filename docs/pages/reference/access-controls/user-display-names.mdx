---
title: User Display Names
description: Explains how Teleport derives human-readable display names for users from identity-provider attributes.
keywords: [user,display name,idp,sso,scim]
tags:
 - reference
 - zero-trust
---

A *display name* is a human-readable name that Teleport derives for a user at
read time from identity-provider attributes already present on the user.
Display names are presentation only: the Teleport **username** remains the
stable identifier and is always shown alongside the display name. There is no
display-name field to set and nothing extra is stored. Teleport builds the name
from the user's traits and, for SCIM-provisioned users, their synced SCIM
attributes.

## Where display names appear

Display names appear in the Teleport Web UI on the **Users** page and in the
user details panel. The username is always shown as well, so a user is never
shown by a name alone.

## How Teleport derives a display name

Teleport fills two values independently, a **primary** value (a name) and a
**secondary** value (an email), from the attributes it finds among the sources
below. Because the two are resolved separately, a user may show a name with no
email, an email with no name, or neither.

| Source | Primary (name) | Secondary (email) | Where to set it |
|--------|----------------|-------------------|-----------------|
| Okta sync | the Okta profile's `displayName`, or `givenName` + `familyName`, or `firstName` + `lastName` | the profile's `email` | [Okta integration](../../identity-governance/integrations/okta/okta.mdx) |
| Microsoft Entra ID | the display-name claim, or the [given-name and surname claims](https://learn.microsoft.com/en-us/entra/identity-platform/reference-saml-tokens) | the email-address claim | [Entra ID integration](../../identity-governance/integrations/entra-id/entra-id.mdx) |
| SAML / OIDC SSO | `displayName`, `name`, `givenName` + `familyName` (or `given_name` + `family_name`), or `firstName` + `lastName` (or `first_name` + `last_name`) | `email` | [authentication connector](../../zero-trust-access/sso/integrate-idp/integrate-idp.mdx) |
| SCIM | `displayName`, or `name.givenName` + `name.familyName` | the SCIM email marked [`"primary": true`](https://datatracker.ietf.org/doc/html/rfc7643#section-8.2), or the first listed email | [SCIM integration](../../identity-governance/integrations/scim/scim.mdx) |

The Microsoft Entra ID display-name and email-address claims correspond to the
SAML claim URIs `http://schemas.microsoft.com/identity/claims/displayname` and
`http://schemas.xmlsoap.org/ws/2005/05/identity/claims/emailaddress`.

## Behavior notes

- **Username fallback.** If none of these attributes are present on a user, both
  values are empty and the Web UI shows only the username. This is why some
  users - for example local users, or SSO users whose provider sends no name - appear without a display name; the feature does not give every user a name.
- **No duplicate of the username.** A value equal to the username is
  dropped, so you never see a name like `alice (alice)`.

## Influencing a user's display name

To change how a user appears, map the attribute you want into that user's traits
through your SSO connector, or provision it through SCIM; use the links in the
table above to reach the page for your provider. Because display names are
presentation only, the username is unchanged and remains the stable reference
for roles, RBAC and audit logging.
