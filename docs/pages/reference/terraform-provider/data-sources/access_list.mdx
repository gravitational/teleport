---
title: Reference for the teleport_access_list Terraform data-source
sidebar_label: access_list
description: This page describes the supported values of the teleport_access_list data-source of the Teleport Terraform provider.
labels:
 - reference
 - platform-wide
---

{/*Auto-generated file. Do not edit.*/}
{/*To regenerate, navigate to integrations/terraform and run `make docs`.*/}





{/*  schema generated by tfplugindocs */}
## Schema

### Optional

- `header` (Attributes) header is the header for the resource. (see [below for nested schema](#nested-schema-for-header))
- `spec` (Attributes) spec is the specification for the Access List. (see [below for nested schema](#nested-schema-for-spec))

### Nested Schema for `header`

Required:

- `version` (String) Version is the API version used to create the resource. It must be specified. Based on this version, Teleport will apply different defaults on resource creation or deletion. It must be an integer prefixed by "v". For example: `v1`

Optional:

- `kind` (String) kind is a resource kind.
- `metadata` (Attributes) metadata is resource metadata. (see [below for nested schema](#nested-schema-for-headermetadata))
- `sub_kind` (String) sub_kind is an optional resource sub kind, used in some resources.

### Nested Schema for `header.metadata`

Required:

- `name` (String) name is an object name.

Optional:

- `description` (String) description is object description.
- `expires` (String) expires is a global expiry time header can be set on any resource in the system.
- `labels` (Map of String) labels is a set of labels.
- `namespace` (String) namespace is object namespace. The field should be called "namespace" when it returns in Teleport 2.4.
- `revision` (String) revision is an opaque identifier which tracks the versions of a resource over time. Clients should ignore and not alter its value but must return the revision in any updates of a resource.



### Nested Schema for `spec`

Required:

- `grants` (Attributes) grants describes the access granted by membership to this Access List. (see [below for nested schema](#nested-schema-for-specgrants))
- `owners` (Attributes List) owners is a list of owners of the Access List. (see [below for nested schema](#nested-schema-for-specowners))

Optional:

- `audit` (Attributes) audit describes the frequency that this Access List must be audited. (see [below for nested schema](#nested-schema-for-specaudit))
- `description` (String) description is an optional plaintext description of the Access List.
- `membership_requires` (Attributes) membership_requires describes the requirements for a user to be a member of the Access List. For a membership to an Access List to be effective, the user must meet the requirements of Membership_requires and must be in the members list. (see [below for nested schema](#nested-schema-for-specmembership_requires))
- `owner_grants` (Attributes) owner_grants describes the access granted by owners to this Access List. (see [below for nested schema](#nested-schema-for-specowner_grants))
- `ownership_requires` (Attributes) ownership_requires describes the requirements for a user to be an owner of the Access List. For ownership of an Access List to be effective, the user must meet the requirements of ownership_requires and must be in the owners list. (see [below for nested schema](#nested-schema-for-specownership_requires))
- `template_config` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_config))
- `title` (String) title is a plaintext short description of the Access List.
- `type` (String) type can be an empty string which denotes a regular Access List, "scim" which represents an Access List created from SCIM group or "static" for Access Lists managed by IaC tools.

### Nested Schema for `spec.grants`

Optional:

- `roles` (List of String) roles are the roles that are granted to users who are members of the Access List.
- `traits` (Attributes List) traits are the traits that are granted to users who are members of the Access List. (see [below for nested schema](#nested-schema-for-specgrantstraits))

### Nested Schema for `spec.grants.traits`

Optional:

- `key` (String) key is the name of the trait.
- `values` (List of String) values is the list of trait values.



### Nested Schema for `spec.owners`

Optional:

- `description` (String) description is the plaintext description of the owner and why they are an owner.
- `membership_kind` (Number) membership_kind describes the type of membership, either `MEMBERSHIP_KIND_USER` or `MEMBERSHIP_KIND_LIST`.
- `name` (String) name is the username of the owner.


### Nested Schema for `spec.audit`

Optional:

- `next_audit_date` (String) next_audit_date is when the next audit date should be done by.
- `notifications` (Attributes) notifications is the configuration for notifying users. (see [below for nested schema](#nested-schema-for-specauditnotifications))
- `recurrence` (Attributes) recurrence is the recurrence definition (see [below for nested schema](#nested-schema-for-specauditrecurrence))

### Nested Schema for `spec.audit.notifications`

Optional:

- `start` (String) start specifies when to start notifying users that the next audit date is coming up.


### Nested Schema for `spec.audit.recurrence`

Optional:

- `day_of_month` (Number) day_of_month is the day of month that reviews will be scheduled on. Supported values are 0, 1, 15, and 31.
- `frequency` (Number) frequency is the frequency of reviews. This represents the period in months between two reviews. Supported values are 0, 1, 3, 6, and 12.



### Nested Schema for `spec.membership_requires`

Optional:

- `roles` (List of String) roles are the user roles that must be present for the user to obtain access.
- `traits` (Attributes List) traits are the traits that must be present for the user to obtain access. (see [below for nested schema](#nested-schema-for-specmembership_requirestraits))

### Nested Schema for `spec.membership_requires.traits`

Optional:

- `key` (String) key is the name of the trait.
- `values` (List of String) values is the list of trait values.



### Nested Schema for `spec.owner_grants`

Optional:

- `roles` (List of String) roles are the roles that are granted to users who are members of the Access List.
- `traits` (Attributes List) traits are the traits that are granted to users who are members of the Access List. (see [below for nested schema](#nested-schema-for-specowner_grantstraits))

### Nested Schema for `spec.owner_grants.traits`

Optional:

- `key` (String) key is the name of the trait.
- `values` (List of String) values is the list of trait values.



### Nested Schema for `spec.ownership_requires`

Optional:

- `roles` (List of String) roles are the user roles that must be present for the user to obtain access.
- `traits` (Attributes List) traits are the traits that must be present for the user to obtain access. (see [below for nested schema](#nested-schema-for-specownership_requirestraits))

### Nested Schema for `spec.ownership_requires.traits`

Optional:

- `key` (String) key is the name of the trait.
- `values` (List of String) values is the list of trait values.



### Nested Schema for `spec.template_config`

Optional:

- `long_term` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configlong_term))
- `short_term` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configshort_term))

### Nested Schema for `spec.template_config.long_term`

Required:

- `allow` (Attributes) access_condition defines access to resources and its principals. (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallow))

### Nested Schema for `spec.template_config.long_term.allow`

Optional:

- `application` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowapplication))
- `database` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowdatabase))
- `git_server` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowgit_server))
- `kubernetes` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowkubernetes))
- `server` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowserver))
- `windows_desktop` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowwindows_desktop))

### Nested Schema for `spec.template_config.long_term.allow.application`

Required:

- `labels` (Attributes List) labels define access to applications. (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowapplicationlabels))

Optional:

- `aws_role_arns` (List of String) AWSRoleARNs is a list of AWS role ARNs access.
- `azure_identities` (List of String) AzureIdentities is a list of Azure identities access.
- `gcp_service_accounts` (List of String) GCPServiceAccounts is a list of GCP service accounts access.
- `mcp` (Attributes) MCPPermissions defines MCP servers related permissions. (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowapplicationmcp))

### Nested Schema for `spec.template_config.long_term.allow.application.labels`

Optional:

- `name` (String) The name of the label.
- `values` (List of String) The values associated with the label.


### Nested Schema for `spec.template_config.long_term.allow.application.mcp`

Optional:

- `tools` (List of String) Tools defines the list of tools allowed or denied for this role. Each entry can be a literal string, a glob pattern (e.g. "prefix_*"), or a regular expression (must start with '^' and end with '$'). If the list is empty, no tools are allowed.



### Nested Schema for `spec.template_config.long_term.allow.database`

Required:

- `labels` (Attributes List) labels define access to databases. (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowdatabaselabels))

Optional:

- `names` (List of String) names is a list of connectable database names.
- `users` (List of String) users is a list of connectable databases users.

### Nested Schema for `spec.template_config.long_term.allow.database.labels`

Optional:

- `name` (String) The name of the label.
- `values` (List of String) The values associated with the label.



### Nested Schema for `spec.template_config.long_term.allow.git_server`

Optional:

- `permissions` (Attributes List) permissions defines GitHub integration related permissions. (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowgit_serverpermissions))

### Nested Schema for `spec.template_config.long_term.allow.git_server.permissions`

Optional:

- `orgs` (List of String)



### Nested Schema for `spec.template_config.long_term.allow.kubernetes`

Required:

- `labels` (Attributes List) labels define access to kubernetes. (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowkuberneteslabels))

Optional:

- `groups` (List of String) groups is a list of kubernetes groups.
- `resources` (Attributes List) resources is a list of Kubernetes Resources. (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowkubernetesresources))
- `users` (List of String) users is a list of impersonable kubernetes users

### Nested Schema for `spec.template_config.long_term.allow.kubernetes.labels`

Optional:

- `name` (String) The name of the label.
- `values` (List of String) The values associated with the label.


### Nested Schema for `spec.template_config.long_term.allow.kubernetes.resources`

Optional:

- `api_group` (String) APIGroup specifies the Kubernetes API group of the Kubernetes resource. It supports wildcards.
- `kind` (String) Kind specifies the Kubernetes Resource type.
- `name` (String) Name is the resource name. It supports wildcards.
- `namespace` (String) Namespace is the resource namespace. It supports wildcards.
- `verbs` (List of String) Verbs are the allowed Kubernetes verbs for the following resource.



### Nested Schema for `spec.template_config.long_term.allow.server`

Required:

- `labels` (Attributes List) labels define access to servers. (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowserverlabels))

Optional:

- `logins` (List of String) logins is a list of *nix system logins.

### Nested Schema for `spec.template_config.long_term.allow.server.labels`

Optional:

- `name` (String) The name of the label.
- `values` (List of String) The values associated with the label.



### Nested Schema for `spec.template_config.long_term.allow.windows_desktop`

Required:

- `labels` (Attributes List) labels define access to windows desktops. (see [below for nested schema](#nested-schema-for-spectemplate_configlong_termallowwindows_desktoplabels))

Optional:

- `logins` (List of String) logins is a list of desktop login names.

### Nested Schema for `spec.template_config.long_term.allow.windows_desktop.labels`

Optional:

- `name` (String) The name of the label.
- `values` (List of String) The values associated with the label.





### Nested Schema for `spec.template_config.short_term`

Required:

- `allow` (Attributes) access_condition defines access to resources and its principals. (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallow))

### Nested Schema for `spec.template_config.short_term.allow`

Optional:

- `application` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowapplication))
- `database` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowdatabase))
- `git_server` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowgit_server))
- `kubernetes` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowkubernetes))
- `server` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowserver))
- `windows_desktop` (Attributes) (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowwindows_desktop))

### Nested Schema for `spec.template_config.short_term.allow.application`

Required:

- `labels` (Attributes List) labels define access to applications. (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowapplicationlabels))

Optional:

- `aws_role_arns` (List of String) AWSRoleARNs is a list of AWS role ARNs access.
- `azure_identities` (List of String) AzureIdentities is a list of Azure identities access.
- `gcp_service_accounts` (List of String) GCPServiceAccounts is a list of GCP service accounts access.
- `mcp` (Attributes) MCPPermissions defines MCP servers related permissions. (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowapplicationmcp))

### Nested Schema for `spec.template_config.short_term.allow.application.labels`

Optional:

- `name` (String) The name of the label.
- `values` (List of String) The values associated with the label.


### Nested Schema for `spec.template_config.short_term.allow.application.mcp`

Optional:

- `tools` (List of String) Tools defines the list of tools allowed or denied for this role. Each entry can be a literal string, a glob pattern (e.g. "prefix_*"), or a regular expression (must start with '^' and end with '$'). If the list is empty, no tools are allowed.



### Nested Schema for `spec.template_config.short_term.allow.database`

Required:

- `labels` (Attributes List) labels define access to databases. (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowdatabaselabels))

Optional:

- `names` (List of String) names is a list of connectable database names.
- `users` (List of String) users is a list of connectable databases users.

### Nested Schema for `spec.template_config.short_term.allow.database.labels`

Optional:

- `name` (String) The name of the label.
- `values` (List of String) The values associated with the label.



### Nested Schema for `spec.template_config.short_term.allow.git_server`

Optional:

- `permissions` (Attributes List) permissions defines GitHub integration related permissions. (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowgit_serverpermissions))

### Nested Schema for `spec.template_config.short_term.allow.git_server.permissions`

Optional:

- `orgs` (List of String)



### Nested Schema for `spec.template_config.short_term.allow.kubernetes`

Required:

- `labels` (Attributes List) labels define access to kubernetes. (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowkuberneteslabels))

Optional:

- `groups` (List of String) groups is a list of kubernetes groups.
- `resources` (Attributes List) resources is a list of Kubernetes Resources. (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowkubernetesresources))
- `users` (List of String) users is a list of impersonable kubernetes users

### Nested Schema for `spec.template_config.short_term.allow.kubernetes.labels`

Optional:

- `name` (String) The name of the label.
- `values` (List of String) The values associated with the label.


### Nested Schema for `spec.template_config.short_term.allow.kubernetes.resources`

Optional:

- `api_group` (String) APIGroup specifies the Kubernetes API group of the Kubernetes resource. It supports wildcards.
- `kind` (String) Kind specifies the Kubernetes Resource type.
- `name` (String) Name is the resource name. It supports wildcards.
- `namespace` (String) Namespace is the resource namespace. It supports wildcards.
- `verbs` (List of String) Verbs are the allowed Kubernetes verbs for the following resource.



### Nested Schema for `spec.template_config.short_term.allow.server`

Required:

- `labels` (Attributes List) labels define access to servers. (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowserverlabels))

Optional:

- `logins` (List of String) logins is a list of *nix system logins.

### Nested Schema for `spec.template_config.short_term.allow.server.labels`

Optional:

- `name` (String) The name of the label.
- `values` (List of String) The values associated with the label.



### Nested Schema for `spec.template_config.short_term.allow.windows_desktop`

Required:

- `labels` (Attributes List) labels define access to windows desktops. (see [below for nested schema](#nested-schema-for-spectemplate_configshort_termallowwindows_desktoplabels))

Optional:

- `logins` (List of String) logins is a list of desktop login names.

### Nested Schema for `spec.template_config.short_term.allow.windows_desktop.labels`

Optional:

- `name` (String) The name of the label.
- `values` (List of String) The values associated with the label.

