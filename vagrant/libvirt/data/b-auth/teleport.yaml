# Auth-only config
teleport:
  nodename: auth-node
  log:
    output: stderr
    severity: INFO

auth_service:
  enabled: yes
  cluster_name: cluster-a.local.com
  listen_addr: 0.0.0.0:3025

ssh_service:
  enabled: no

proxy_service:
  enabled: no
