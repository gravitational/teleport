# Auth-only config
teleport:
  nodename: auth-node
  log:
    output: stderr
    severity: INFO

auth_service:
  enabled: yes
  cluster_name: cluster-b
  listen_addr: 0.0.0.0:3025

ssh_service:
  enabled: yes

proxy_service:
  enabled: yes
