# Auth-only config
teleport:
  nodename: auth-a
  log:
    output: stderr
    severity: INFO

auth_service:
  enabled: yes
  cluster_name: cluster-a
  listen_addr: 0.0.0.0:3025
  tokens: ["proxy:helloworld", "node:node"]

ssh_service:
  enabled: yes

proxy_service:
  enabled: yes
