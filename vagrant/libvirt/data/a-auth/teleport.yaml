# Auth for cluster-A
teleport:
  nodename: auth-a
  log:
    output: stderr
    severity: INFO

auth_service:
  enabled: yes
  cluster_name: cluster-a
  listen_addr: 0.0.0.0:3025
  trusted_clusters:
      - /opt/teleport/cluster-b

ssh_service:
  enabled: yes

proxy_service:
  enabled: yes
