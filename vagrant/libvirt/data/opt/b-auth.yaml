# Auth for cluster-B
teleport:
  nodename: auth-b
  log:
    output: stderr
    severity: INFO

auth_service:
  enabled: yes
  cluster_name: cluster-b
  trusted_clusters:
      - keyfile: /opt/shared/cluster-a

ssh_service:
  enabled: yes

proxy_service:
  enabled: yes
