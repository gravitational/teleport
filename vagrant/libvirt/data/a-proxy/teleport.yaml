# Proxy-only config
teleport:
  nodename: proxy-a
  auth_servers: ["10.0.10.10:3025"]
  auth_token: helloworld
  log:
    output: stderr
    severity: ERROR

auth_service:
  enabled: no
  listen_addr: 0.0.0.0:3025

ssh_service:
  enabled: no

proxy_service:
  enabled: yes
